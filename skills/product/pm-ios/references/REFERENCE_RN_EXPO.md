# React Native / Expo 參考

- 此檔只在需求明確指定 `React Native`、`Expo`、`Expo Router`，或專案屬於 iOS 優先跨平台 app 時讀取。
- 導航預設優先考慮 `Expo Router`。
- 版面必須明確處理 `safe area`。
- 元件選型要考慮跨平台一致性，不直接照搬原生 iOS 元件命名。
- 若某設計需要自訂 native module，應標記為高風險。
