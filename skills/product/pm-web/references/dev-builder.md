# Dev Builder

- 任務：根據 `Product-Spec.md` 建立或迭代 Web 專案。
- 若沒有 `Product-Spec.md`，先要求執行 `/prd`。
- 優先簡單方案，不過度工程。
- 只改目前任務相關檔案；不要順手升級依賴、全域格式化或無關重命名。
- 新專案需檢查 `.gitignore`，避免提交 `AGENTS.md`、`CLAUDE.md`、`.tasks/`、`.spec/`、`spec/`。
- 涉及敏感金鑰時，預設走伺服器端，不把金鑰暴露到前端。
