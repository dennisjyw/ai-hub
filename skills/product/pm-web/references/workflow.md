# Web Workflow

- 一律使用繁體中文（台灣）。
- 流程固定為：`/prd` -> `/ui`（選填）-> `/dev` -> `/check` / `/run`。
- 任何功能、UI、流程變更，先更新 `Product-Spec.md`，再改程式碼。
- `/check`、`/run`、`/status` 先讀 `Product-Spec.md` 與專案現況，再回報下一步。
- 回應先講目前階段、缺什麼、下一步。
